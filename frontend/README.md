# CineCity Frontend

This folder contains the React-based frontend for the DC project 3.  
The UI is built using **React, TailwindCSS, and React Router**, and connects to the backend API (buducnost).

---

## 🚀 Tech Stack
- **React (Create React App)**
- **TailwindCSS** for styling
- **React Router** for page navigation
- **Reusable components** (MovieCard, CityPage, Profile pages, etc.)
